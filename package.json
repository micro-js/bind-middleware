{
  "name": "@f/wrap-redux-ware",
  "description": "Convert redux middleware into a regular function that takes an &#x60;action&#x60;.",
  "repository": "git://github.com/micro-js/wrap-redux-ware.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/clone-obj": "^1.1.2",
    "@f/compose-redux-ware": "^1.0.0",
    "@f/is-array": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
