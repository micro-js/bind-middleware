{
  "name": "@f/bind-middleware",
  "description": "Bind middleware to a context and optionally a next function..",
  "repository": "git://github.com/micro-js/bind-middleware.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/clone-obj": "^1.1.2",
    "@f/compose-middleware": "^2.0.0",
    "@f/is-array": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
